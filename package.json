{
  "name": "uballers-test",
  "version": "1.0.0",
  "description": "server for db api",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "reifocs",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "lowdb": "^1.0.0"
  }
}
